let projectName = bitcnt;

def bitcnt : Instruction {
    def tmp : local;
    if(rs1 == 100) {
        let tmp = 0;
        let rd = 0;
        while(rd != 100) {
            let rd += 1;
        }
    } else let rd = bitcnt(rs1, rs2);
}