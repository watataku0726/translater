def memLoadDataIn : Register;

def loadata : Instruction {
    def ready : local;
    let ready = 0;
    while(ready == 0) {
        let dataOut = 0;
        let ready = loadData(memLoadDataIn);
        if(ready == 0) load<memLoadDataIn, memAddrOut>;
    }
    let rd = ready;
}