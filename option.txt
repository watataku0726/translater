def memLoadDataIn : Register;

def loadata : Instruction {
    def ready : local;
    let ready = 0;
    let ready = 1 < ready;
    let ready = ready > 1 && ready;
    let ready = loadData(memLoadDataIn);
    while(ready != 0) {
        let dataOut = 0;
        let ready = loadData(memLoadDataIn);
        if(opOut == 6) load<memLoadDataIn, memAddrOut>;
    }
    let rd = ready;
}